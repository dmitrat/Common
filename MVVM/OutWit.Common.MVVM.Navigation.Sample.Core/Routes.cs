namespace OutWit.Common.MVVM.Navigation.Sample.Core
{
    /// <summary>
    /// The application's route keys. Plain strings, declared in one place — the package has
    /// no opinion about them.
    /// </summary>
    public static class Routes
    {
        public const string STUDIES = "studies";

        public const string STUDY = "study";

        public const string SETTINGS = "settings";

        /// <summary>
        /// Registered by the Reports module, not by the shell.
        /// </summary>
        public const string REPORTS = "reports";
    }
}
