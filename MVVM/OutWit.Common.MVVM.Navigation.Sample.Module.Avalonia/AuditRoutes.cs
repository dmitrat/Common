namespace OutWit.Common.MVVM.Navigation.Sample.Module.Avalonia
{
    /// <summary>
    /// The module's own route keys. The application never sees this class — it lives in a DLL
    /// the application does not reference.
    /// </summary>
    public static class AuditRoutes
    {
        public const string MAIN = "audit";
    }
}
